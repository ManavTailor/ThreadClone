//  @type {import('next').NextConfig} *
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['img.icons8.com' , 'https://scontent.cdninstagram.com', 'scontent.cdninstagram.com'],
  },
}

module.exports = nextConfig
